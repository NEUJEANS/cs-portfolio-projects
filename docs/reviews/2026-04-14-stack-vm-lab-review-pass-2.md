# Review pass 2: stack-vm-lab

- issue found: the README referenced `examples/max2.svm`, but no example file existed yet
- fix applied: added a real `examples/max2.svm` sample so the docs stay runnable
- validation: used the CLI disassemble path against the example file and kept the test coverage in place
