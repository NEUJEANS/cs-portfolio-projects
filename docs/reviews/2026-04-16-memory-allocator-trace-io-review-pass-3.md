# Review pass 3 - docs and resume workflow audit

## Focus
- README discoverability for the new feature
- whether the slice is resumable for future cron runs

## Checks
- README now documents `--trace-in` and `--trace-out` with a concrete example
- checklist and learning notes were added under `docs/checklists/` and `docs/learning/`
- exported traces are replayable JSON bundles, so later slices can reuse them directly as regression/demo fixtures

## Issues found
- none
