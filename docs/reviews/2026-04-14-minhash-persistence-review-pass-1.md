# Review pass 1 - minhash persistence slice

## Checked
- overall CLI shape and whether the new commands fit the existing project style
- index schema contents and whether persisted metadata is sufficient for reproducible reruns
- compatibility of new helpers with the existing compare/corpus code paths

## Issue found
- README and checklist still described persistence/benchmarking as future work.

## Fix applied
- updated the project roadmap/docs after implementation so the portfolio story matches the code.
