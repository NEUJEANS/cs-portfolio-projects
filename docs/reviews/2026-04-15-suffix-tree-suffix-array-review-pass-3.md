# Review pass 3 — suffix-tree suffix-array benchmark slice

## Focus
Docs and artifact audit.

## Checks
- verified the README now mentions the suffix-array benchmark baseline and explains why the implementation is intentionally naive
- regenerated `artifacts/suffix-tree-benchmark.csv` so committed numbers include the new method rows
- confirmed the new learning note records the binary-search tradeoff and a quick self-test example

## Result
- docs and artifact set are consistent with the implementation
