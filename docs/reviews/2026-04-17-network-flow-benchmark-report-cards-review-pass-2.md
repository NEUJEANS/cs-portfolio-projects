# Network-flow benchmark report cards review - pass 2

## Focus
CLI/docs/checklist consistency for benchmark artifact export.

## Findings
- Verified the new `benchmark --markdown-out --svg-out` flow is documented in the project README.
- Verified the project checklist and repo-level checklist both now record benchmark report-card export as completed work.
- Verified the committed artifact paths listed in the README match the files generated under `docs/artifacts/network-flow-lab/`.

## Fix applied
- No code fix was needed after this pass; the docs and checklist updates were already consistent.

## Result
- The slice is resumable from README/checklist context without reopening the implementation details first.
