# Two-phase commit lab — 2026-04-20 HTML comparison dashboard slice

- [x] confirm the repo is synced with `origin/main` before editing
- [x] inspect the latest two-phase-commit-lab state and continue the next planned comparison-dashboard slice
- [x] skip external web research because this slice is presentation-focused and reuses the already-validated 2PC vs saga model
- [x] do a short HTML/CSS artifact refresh by reviewing existing static dashboard patterns already shipped elsewhere in the repo
- [x] update the project checklist with the new slice target and next-step options
- [x] add a first-class `compare --html-out` export with a compact static dashboard over the existing comparison payload
- [x] commit representative HTML artifacts for both the classic blocked crash and the peer-visible-decision blocked crash
- [x] add/extend regression tests for HTML rendering plus CLI artifact generation
- [x] review the slice at least 3 times, fix issues found, then rerun verification
- [x] run secret scan, commit, push, and append a wrap-up
