# MVCC isolation lab — 2026-04-20 gallery/catalog slice

- [x] continue from the existing MVCC artifact pipeline instead of starting a new project
- [x] confirm no extra web research was needed because this slice extends the already-documented dashboard/export model
- [x] confirm no extra language/tool refresh was needed beyond the existing Python + argparse + static HTML test coverage
- [x] add a resumable checklist entry for the gallery/catalog slice
- [x] implement a `catalog` command that regenerates per-scenario Markdown/HTML/SVG artifacts and a single landing page
- [x] add tests for catalog rendering and CLI output paths
- [x] refresh the README plus checklist docs to advertise the new portfolio entry point
- [x] run tests, review at least 3 times, secret-scan, commit, push, and add a wrap-up
