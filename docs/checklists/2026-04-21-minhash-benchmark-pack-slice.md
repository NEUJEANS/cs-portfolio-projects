# MinHash benchmark pack slice

- [x] confirm repo sync before editing
- [x] resume the queued MinHash follow-up around richer benchmark dataset packs
- [x] skip external web research because this slice extends the existing in-repo benchmark/export architecture
- [x] self-test the current benchmark API against tiny, medium, and noisy draft corpora before editing
- [x] add `write-benchmark-pack` so the repo can generate committed tiny, medium, and noisy benchmark scenarios in one step
- [x] record expected recall ranges alongside observed benchmark results for each scenario
- [x] export checked-in JSON, CSV, Markdown, and HTML summaries for the benchmark pack
- [x] expand repo-level tests for the benchmark-pack writer, force-safety, and CLI JSON flow
- [x] run targeted validation and regenerate the checked-in artifact bundle
- [x] complete at least 3 review passes and fix issues found
- [ ] run secret scan before push
- [ ] commit, push, and add wrap-up
- [ ] next: add language-aware literal buckets for lists, dicts, template strings, and JSX inline objects in code mode
