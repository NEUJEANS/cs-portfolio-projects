# graph-routing-negative-cycle-lab SVG summary card slice

- [x] confirm repo branch/remote status, fetch, and verify local `main` matches `origin/main` before editing
- [x] choose the next unfinished visual slice for the project: add a compact SVG route-diff summary card on top of the existing Markdown + HTML comparison flow
- [x] do brief SVG research on `viewBox` sizing and accessible `<title>` usage
- [x] do a short Python/SVG refresh and self-test for deterministic static text layout
- [x] add a checked-in slice checklist so the work stays resumable
- [x] implement `--export-compare-svg` and a deterministic `render_svg_comparison()` flow
- [x] keep the SVG artifact recruiter-friendly: summary metrics, edge preview, changed-route highlights, and static footer copy
- [x] extend automated tests for direct export, CLI export, and compare-graph guardrails
- [x] generate and commit a sample SVG artifact under `docs/artifacts/`
- [x] complete three review passes and fix the issues found
- [x] rerun focused tests, deterministic artifact checks, and `git diff --check`
- [x] run a secret scan before push
- [x] commit, push, and add a timestamped wrap-up
