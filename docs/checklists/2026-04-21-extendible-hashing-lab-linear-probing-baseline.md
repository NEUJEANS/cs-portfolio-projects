# extendible-hashing-lab linear-probing baseline slice checklist

- [x] sync `main` with `origin/main` and confirm no remote drift before editing
- [x] do a brief research refresh on linear probing, clustering, and tombstone cleanup tradeoffs
- [x] refresh the benchmark-story expectations with a short self-test before coding
- [x] finish the in-progress linear-probing benchmark slice in `extendible_hashing_lab.py`
- [x] make the benchmark suite config explicit for the linear baseline and refresh project docs/checklists
- [x] regenerate committed benchmark JSON/Markdown/HTML/CSV artifacts with the new baseline story
- [x] add regression coverage for linear-probing behavior, summary consistency checks, and CSV export fields
- [x] run tests, benchmark export smoke checks, determinism checks, and `git diff --check`
- [x] complete at least 3 review passes and fix the issues found
- [x] run a secret scan, commit, push, and write the wrap-up
