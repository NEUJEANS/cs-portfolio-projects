# extendible-hashing-lab clustering-preset checklist — 2026-04-21

- [x] confirm repo sync state before editing (`git fetch`, ahead/behind check, clean status)
- [x] do brief research on primary clustering, tombstones, and cleanup rebuild behavior for linear probing
- [x] refresh the linear-probing clustering story with a short self-test note before coding
- [x] add a deterministic benchmark scenario that exaggerates primary clustering and tombstone-cleanup pressure
- [x] update project docs/checklists so the new preset is discoverable and the next follow-up is explicit
- [x] regenerate the committed benchmark artifact bundle
- [x] run compile + focused unit tests and inspect exported benchmark outputs
- [x] complete at least 3 review passes and fix issues found
- [ ] run secret scan, commit, push, and write the wrap-up
