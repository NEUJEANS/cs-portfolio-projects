# 2026-04-22 log-analyzer card PNG slice

- [x] confirm `main` matches `origin/main` before edits
- [x] choose the queued PNG/raster export follow-up for `log-analyzer` instead of switching projects
- [x] do a brief Chrome headless / Python tempfile research check plus a short PNG capture self-test plan before coding
- [x] update checklist/docs so the slice is resumable
- [x] add standalone `--time-bucket-card-png` and `--facet-compare-card-png` exports on top of the existing HTML/SVG card pipeline
- [x] add shared Chrome capture controls and safe temporary-HTML handling for PNG-only runs
- [x] extend automated coverage for PNG helpers, CLI success paths, and validation errors
- [x] regenerate committed sample PNG artifacts for the annotated release-review cards
- [x] run targeted tests and smoke checks
- [x] run at least 3 review passes and fix issues found
- [x] run secret scan before push
- [x] commit, push, and add wrap-up
