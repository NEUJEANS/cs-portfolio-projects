# chord-dht-lab portfolio snapshot bundle learning note

- Quick refresh target: keep the new slice boring and reliable by reusing the existing markdown/csv renderers instead of inventing a new report format.
- Self-test plan: exercise one existing export with `--output` first to confirm the current file-writing helper already gives deterministic newline behavior and parent-directory creation.
